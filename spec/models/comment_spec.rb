require 'rails_helper'

RSpec.describe Comment, type: :model do
	#before do
	#	@user = FactoryBot.create(:user)
	#	@post = FactoruBot.create(:post)
	#	@comment = @user.comments.build(content: "test comment",post_id: @post.ic, user_id: @user.id)
	#end

	it "is valid with content, user_id, post_id"

	it "is invalid without user_id"

	it "is invalid without post_id" 

	it "is invalid without content length more than 50 char" 

	
end
